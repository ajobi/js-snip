/*
 * TODOs:
 * - remove the issues shield
 * - migrate from travis to GitHub Actions
 * - fix resize observer issues in tests
 * */
export * from './types'
export { snip, unsnip } from './snip'
export { getLines, supportsCSSMode } from './utils'
